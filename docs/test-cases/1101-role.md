# Role & Permissions — Test Cases

_Test-case catalog (documentation only; no automated Playwright spec yet). Authored from the React app module at `routes/system-admin/role`. Follows the TC-ID scheme in `docs/test-id-scheme.md`._

**Module:** Role & Permissions
**Frontend route:** `routes/system-admin/role`  •  **URL:** `/system-admin/role`
**Prefix:** `ROLE`
**Default role:** Platform Admin
**Total test cases:** 27

## Test Cases at a Glance
| TC | Title | Priority | Test Type |
| --- | --- | --- | --- |
| TC-ROLE-010001 | หน้า list ของ Role โหลดสำเร็จ | High | Smoke |
| TC-ROLE-010002 | ปุ่ม Add แสดงและกดเข้าหน้า new ได้ | High | Smoke |
| TC-ROLE-010003 | คอลัมน์ Name และ Permissions แสดงค่าถูกต้อง | High | Functional |
| TC-ROLE-010004 | ค้นหา Role ด้วยชื่อแล้วกรองรายการได้ | Medium | Functional |
| TC-ROLE-010005 | ค้นหาคำที่ไม่มีต้องแสดง empty state | Medium | Functional |
| TC-ROLE-010006 | Badge จำนวน Role รวมแสดงตรงกับ total | Low | Functional |
| TC-ROLE-020001 | เปิดหน้า detail/view ของ Role สำเร็จ | High | Smoke |
| TC-ROLE-020002 | Stat tiles แสดง Permissions/Categories/Resources | Medium | Functional |
| TC-ROLE-020003 | Permission matrix แสดง action ที่ถูก grant ถูกต้อง | High | Functional |
| TC-ROLE-030001 | เปิดหน้า new form สำเร็จ | High | Smoke |
| TC-ROLE-030002 | สร้าง Role ขั้นต่ำ (เฉพาะชื่อ ไม่มีสิทธิ์) สำเร็จ | High | CRUD |
| TC-ROLE-030003 | สร้าง Role พร้อมเลือกสิทธิ์รายตัว (checkbox) สำเร็จ | High | CRUD |
| TC-ROLE-030004 | สร้าง Role โดย Grant All ทั้ง category สำเร็จ | High | CRUD |
| TC-ROLE-040001 | แก้ชื่อ Role แล้ว save สำเร็จ | High | CRUD |
| TC-ROLE-040002 | เพิ่มสิทธิ์ให้ Role เดิม (add) แล้ว save สำเร็จ | High | CRUD |
| TC-ROLE-040003 | ถอนสิทธิ์ออกจาก Role เดิม (remove) แล้ว save สำเร็จ | High | CRUD |
| TC-ROLE-040004 | ยกเลิกการแก้ไขที่ค้างต้องเด้ง Discard dialog | Medium | Functional |
| TC-ROLE-050001 | เปิด delete dialog แล้ว cancel — Role ยังอยู่ | Medium | Functional |
| TC-ROLE-050002 | ลบ Role จากหน้า list สำเร็จ | High | CRUD |
| TC-ROLE-050003 | ลบ Role จากหน้า detail (Hero delete) สำเร็จ | High | CRUD |
| TC-ROLE-100001 | ผู้ใช้ที่ไม่มีสิทธิ์เข้าถึง /system-admin/role ต้องถูกบล็อก | High | Authorization |
| TC-ROLE-100002 | เข้าหน้า role โดยไม่ได้ login ต้อง redirect ไป /login | High | Auth-guard |
| TC-ROLE-200001 | บันทึก Role โดยไม่กรอกชื่อต้องแสดง error | High | Validation |
| TC-ROLE-200002 | ชื่อ Role เกิน 100 ตัวอักษรต้องถูกจำกัด | Medium | Validation |
| TC-ROLE-400001 | toggle column (เช่น View ทั้ง category) ติ๊ก/เอาออกทั้งคอลัมน์ | Medium | Functional |
| TC-ROLE-400002 | ตัวกรอง Show Granted / Show Missing ใน picker ทำงาน | Medium | Functional |
| TC-ROLE-400003 | ค้นหา resource ใน permission picker กรองเฉพาะที่ตรงคำ | Medium | Functional |

---
## TC-ROLE-010001 — หน้า list ของ Role โหลดสำเร็จ
**Priority:** High · **Test Type:** Smoke
**Preconditions**
ล็อกอินเป็น Platform Admin ที่มีสิทธิ์เข้าถึงโมดูล System Admin
**Steps**
1. ไปที่ URL `/system-admin/role`
2. รอให้หน้าโหลดเสร็จ
**Expected**
URL ตรงกับ `/system-admin/role`, หัวข้อ (title) ของหน้า Role แสดง และตาราง DataGrid หรือ empty state ปรากฏภายใน 10 วินาที

---
## TC-ROLE-010002 — ปุ่ม Add แสดงและกดเข้าหน้า new ได้
**Priority:** High · **Test Type:** Smoke
**Preconditions**
อยู่ที่หน้า `/system-admin/role`
**Steps**
1. มองหาปุ่ม Add (ไอคอน +) มุมขวาบน
2. คลิกปุ่ม Add
**Expected**
ปุ่ม Add visible และเมื่อคลิกแล้ว navigate ไปยัง `/system-admin/role/new`

---
## TC-ROLE-010003 — คอลัมน์ Name และ Permissions แสดงค่าถูกต้อง
**Priority:** High · **Test Type:** Functional
**Preconditions**
มี Role อย่างน้อย 1 รายการใน DB
**Steps**
1. ไปที่หน้า list
2. ตรวจสอบหัวคอลัมน์ Name และ Permissions
3. อ่านค่าในแถวแรก
**Expected**
คอลัมน์ Name แสดงชื่อ Role และคอลัมน์ Permissions แสดงข้อความรูปแบบ `<n> permissions` ตรงกับจำนวนสิทธิ์ของแถวนั้น

---
## TC-ROLE-010004 — ค้นหา Role ด้วยชื่อแล้วกรองรายการได้
**Priority:** Medium · **Test Type:** Functional
**Preconditions**
มี Role อย่างน้อย 1 รายการที่ทราบชื่อ
**Steps**
1. ไปที่หน้า list
2. พิมพ์คำค้นในช่องค้นหา (SearchInput) แล้วกด Enter
**Expected**
ตารางแสดงเฉพาะ Role ที่ชื่อตรงกับคำค้น

---
## TC-ROLE-010005 — ค้นหาคำที่ไม่มีต้องแสดง empty state
**Priority:** Medium · **Test Type:** Functional
**Preconditions**
อยู่ที่หน้า list
**Steps**
1. พิมพ์คำค้นที่ไม่มีในระบบ (เช่น `__NOPE__zzz`) แล้วกด Enter
**Expected**
ตารางไม่มีแถว และแสดง empty-state placeholder ภายใน 10 วินาที

---
## TC-ROLE-010006 — Badge จำนวน Role รวมแสดงตรงกับ total
**Priority:** Low · **Test Type:** Functional
**Preconditions**
มี Role อย่างน้อย 1 รายการ
**Steps**
1. ไปที่หน้า list
2. อ่านตัวเลข Badge ที่อยู่ข้างหัวข้อ Role
**Expected**
Badge แสดงตัวเลขรวม (totalRecords) ในรูปแบบมี comma เช่น `1,024` และตรงกับจำนวนระเบียนจริง

---
## TC-ROLE-020001 — เปิดหน้า detail/view ของ Role สำเร็จ
**Priority:** High · **Test Type:** Smoke
**Preconditions**
มี Role อย่างน้อย 1 รายการ
**Steps**
1. ไปที่หน้า list
2. คลิกชื่อ Role ในคอลัมน์ Name
**Expected**
navigate ไปยัง `/system-admin/role/<id>`, ฟอร์มอยู่ในโหมด view (read-only), Hero แสดงชื่อ Role และปุ่ม Edit ปรากฏ

---
## TC-ROLE-020002 — Stat tiles แสดง Permissions/Categories/Resources
**Priority:** Medium · **Test Type:** Functional
**Preconditions**
เปิดหน้า detail ของ Role ที่มีสิทธิ์ติดอยู่บ้าง
**Steps**
1. เปิดหน้า detail ของ Role
2. ดู stat tiles 3 ใบด้านบน
**Expected**
Tile แรกแสดงจำนวน permissions ที่เลือก (selectedPermissionCount), tile ที่สองแสดงจำนวน categories ของ catalog และ tile ที่สามแสดงจำนวน resources

---
## TC-ROLE-020003 — Permission matrix แสดง action ที่ถูก grant ถูกต้อง
**Priority:** High · **Test Type:** Functional
**Preconditions**
เปิดหน้า detail ของ Role ที่มีสิทธิ์บางส่วนเปิดอยู่
**Steps**
1. เลื่อนไปที่ section Permissions
2. ดู matrix รายแต่ละ category (เช่น Configuration, Procurement)
3. ตรวจสอบ checkbox ของ action ในคอลัมน์ view/create/update/delete/execute ฯลฯ
**Expected**
Checkbox ที่ตรงกับ permission_id ที่ Role มีอยู่ถูกติ๊ก, ช่องที่ resource ไม่มี action นั้นแสดงเครื่องหมาย `—`, และ checkbox ทั้งหมดเป็น disabled ในโหมด view

---
## TC-ROLE-030001 — เปิดหน้า new form สำเร็จ
**Priority:** High · **Test Type:** Smoke
**Preconditions**
ล็อกอินเป็น Platform Admin
**Steps**
1. ไปที่ `/system-admin/role/new`
**Expected**
ฟอร์มอยู่ในโหมด add, ช่อง Name (input) ว่าง, section Permissions แสดง permission picker และ stat tiles แสดงเลข 0 ที่ tile แรก

---
## TC-ROLE-030002 — สร้าง Role ขั้นต่ำ (เฉพาะชื่อ ไม่มีสิทธิ์) สำเร็จ
**Priority:** High · **Test Type:** CRUD
**Preconditions**
อยู่ที่ `/system-admin/role/new`; ชื่อ Role ที่จะใช้ยังไม่มีใน DB
**Steps**
1. กรอกชื่อ Role ในช่อง Name
2. ไม่เลือกสิทธิ์ใด ๆ
3. กด Save
**Expected**
แสดง toast `createSuccess` และ navigate กลับ `/system-admin/role`; Role ใหม่ปรากฏใน list โดยคอลัมน์ Permissions แสดง `0 permissions`

---
## TC-ROLE-030003 — สร้าง Role พร้อมเลือกสิทธิ์รายตัว (checkbox) สำเร็จ
**Priority:** High · **Test Type:** CRUD
**Preconditions**
อยู่ที่ `/system-admin/role/new`; catalog สิทธิ์โหลดแล้ว
**Steps**
1. กรอกชื่อ Role
2. ใน permission picker ติ๊ก checkbox ของ action รายตัว เช่น `view` ของ resource หนึ่ง
3. ดูค่า stat tile Permissions เพิ่มขึ้น
4. กด Save
**Expected**
toast `createSuccess` ปรากฏ, navigate กลับ list, และ Role ใหม่แสดงจำนวน permissions เท่ากับที่เลือก

---
## TC-ROLE-030004 — สร้าง Role โดย Grant All ทั้ง category สำเร็จ
**Priority:** High · **Test Type:** CRUD
**Preconditions**
อยู่ที่ `/system-admin/role/new`
**Steps**
1. กรอกชื่อ Role
2. ในหัว matrix ของ category หนึ่ง ติ๊ก checkbox `Grant All`
3. ยืนยันว่า action ทุก resource ในคอลัมน์ของ category นั้นถูกติ๊กครบ
4. กด Save
**Expected**
ทุก permission ของ category ถูกเลือก (stat tile เพิ่มตามจำนวน), toast `createSuccess` ปรากฏ และ Role ถูกบันทึกพร้อมสิทธิ์ครบ category

---
## TC-ROLE-040001 — แก้ชื่อ Role แล้ว save สำเร็จ
**Priority:** High · **Test Type:** CRUD
**Preconditions**
มี Role ที่สร้างไว้แล้ว
**Steps**
1. เปิดหน้า detail ของ Role
2. กด Edit เพื่อเข้าโหมด edit
3. แก้ค่าในช่อง Name
4. กด Save
**Expected**
toast `updateSuccess` ปรากฏ, navigate กลับ list และชื่อใหม่แสดงในคอลัมน์ Name

---
## TC-ROLE-040002 — เพิ่มสิทธิ์ให้ Role เดิม (add) แล้ว save สำเร็จ
**Priority:** High · **Test Type:** CRUD
**Preconditions**
มี Role ที่ยังไม่มีสิทธิ์บาง action
**Steps**
1. เปิด detail แล้วกด Edit
2. ติ๊ก checkbox ของ action ที่ยังไม่เลือก (เพิ่มสิทธิ์)
3. สังเกต change-preview แสดง `<n> pending`
4. กด Save
**Expected**
ระบบส่งเฉพาะ permission ที่อยู่ในรายการ add, toast `updateSuccess` ปรากฏ และคอลัมน์ Permissions ใน list เพิ่มจำนวนตามที่เพิ่ม

---
## TC-ROLE-040003 — ถอนสิทธิ์ออกจาก Role เดิม (remove) แล้ว save สำเร็จ
**Priority:** High · **Test Type:** CRUD
**Preconditions**
มี Role ที่มีสิทธิ์ติดอยู่อย่างน้อย 1 action
**Steps**
1. เปิด detail แล้วกด Edit
2. เอา checkbox ของ action ที่ติดอยู่ออก
3. กด Save
**Expected**
ระบบส่ง permission ที่อยู่ในรายการ remove, toast `updateSuccess` ปรากฏ และจำนวน permissions ลดลงตามที่ถอน

---
## TC-ROLE-040004 — ยกเลิกการแก้ไขที่ค้างต้องเด้ง Discard dialog
**Priority:** Medium · **Test Type:** Functional
**Preconditions**
อยู่ในโหมด edit และมีการเปลี่ยนค่าฟอร์มแล้ว (form dirty)
**Steps**
1. แก้ค่าใด ๆ ในฟอร์ม (เช่น Name)
2. กด Cancel หรือกดปุ่ม Back ที่ Hero
**Expected**
Discard dialog (variant warning) ปรากฏ; เมื่อยืนยัน discard ฟอร์ม reset กลับค่าเดิมและออกจากโหมด edit (หรือ navigate กลับ list)

---
## TC-ROLE-050001 — เปิด delete dialog แล้ว cancel — Role ยังอยู่
**Priority:** Medium · **Test Type:** Functional
**Preconditions**
มี Role อย่างน้อย 1 รายการ
**Steps**
1. ไปที่หน้า list
2. เปิด row action ของ Role แล้วเลือก Delete
3. ใน DeleteDialog กด Cancel
**Expected**
Dialog ปิดและแถว Role ยังคงอยู่ใน list (ไม่ถูกลบ)

---
## TC-ROLE-050002 — ลบ Role จากหน้า list สำเร็จ
**Priority:** High · **Test Type:** CRUD
**Preconditions**
มี Role ที่สร้างไว้สำหรับทดสอบ (ไม่ผูกกับผู้ใช้สำคัญ)
**Steps**
1. ไปที่หน้า list
2. เปิด row action ของ Role แล้วเลือก Delete
3. ใน DeleteDialog กดยืนยัน Delete
**Expected**
แสดง toast `deleteSuccess` และแถว Role หายจาก list

---
## TC-ROLE-050003 — ลบ Role จากหน้า detail (Hero delete) สำเร็จ
**Priority:** High · **Test Type:** CRUD
**Preconditions**
เปิดหน้า detail ของ Role ทดสอบในโหมด view
**Steps**
1. เปิด detail ของ Role
2. กดปุ่ม Delete ที่ Hero (แสดงเฉพาะโหมด view)
3. ยืนยันใน DeleteDialog
**Expected**
toast `deleteSuccess` ปรากฏและ navigate กลับ `/system-admin/role`; Role ไม่อยู่ใน list อีก

---
## TC-ROLE-100001 — ผู้ใช้ที่ไม่มีสิทธิ์เข้าถึง /system-admin/role ต้องถูกบล็อก
**Priority:** High · **Test Type:** Authorization
**Preconditions**
ล็อกอินเป็นผู้ใช้ที่ไม่มีสิทธิ์ System Admin (เช่น staff ทั่วไป)
**Steps**
1. พยายามเปิด URL `/system-admin/role` โดยตรง
**Expected**
ระบบไม่อนุญาต — แสดงหน้า unauthorized / redirect ออกจากเส้นทาง หรือ list ไม่โหลดข้อมูล (ไม่เห็นปุ่ม Add / รายการ Role)

---
## TC-ROLE-100002 — เข้าหน้า role โดยไม่ได้ login ต้อง redirect ไป /login
**Priority:** High · **Test Type:** Auth-guard
**Preconditions**
ไม่มี session / ไม่ได้ล็อกอิน
**Steps**
1. เปิด URL `/system-admin/role` โดยตรง
**Expected**
ถูก redirect ไปยัง `/login` และไม่แสดงข้อมูล Role ใด ๆ

---
## TC-ROLE-200001 — บันทึก Role โดยไม่กรอกชื่อต้องแสดง error
**Priority:** High · **Test Type:** Validation
**Preconditions**
อยู่ที่ `/system-admin/role/new`
**Steps**
1. เว้นช่อง Name ให้ว่าง
2. กด Save
**Expected**
แสดงข้อความ error `Name is required` ใต้ช่อง Name, ฟอร์มไม่ถูก submit และไม่มี toast สำเร็จ (zod `application_role_name.min(1)`)

---
## TC-ROLE-200002 — ชื่อ Role เกิน 100 ตัวอักษรต้องถูกจำกัด
**Priority:** Medium · **Test Type:** Validation
**Preconditions**
อยู่ที่ `/system-admin/role/new`
**Steps**
1. พยายามพิมพ์ชื่อยาวเกิน 100 ตัวอักษรในช่อง Name
**Expected**
ค่าในช่อง Name ถูกจำกัดไม่เกิน 100 ตัวอักษร (input `maxLength={100}`)

---
## TC-ROLE-400001 — toggle column (เช่น View ทั้ง category) ติ๊ก/เอาออกทั้งคอลัมน์
**Priority:** Medium · **Test Type:** Functional
**Preconditions**
อยู่ในโหมด add หรือ edit ของ Role; matrix แสดงอยู่
**Steps**
1. ใน matrix ของ category หนึ่ง คลิกหัวคอลัมน์ action (เช่น `View`)
2. สังเกตตัวนับ `on/applicable` ใต้หัวคอลัมน์
3. คลิกหัวคอลัมน์เดิมซ้ำ
**Expected**
ครั้งแรกติ๊ก action นั้นของทุก resource ที่มี action ดังกล่าวในคอลัมน์ (`on === applicable`); คลิกซ้ำเอาออกทั้งคอลัมน์ (`on === 0`)

---
## TC-ROLE-400002 — ตัวกรอง Show Granted / Show Missing ใน picker ทำงาน
**Priority:** Medium · **Test Type:** Functional
**Preconditions**
อยู่ในโหมด edit ของ Role ที่มีสิทธิ์ติดอยู่บางส่วน
**Steps**
1. ในแถบ toolbar ของ picker คลิก `Show Granted`
2. ดู resource ที่เหลือ
3. คลิก `Show Missing`
**Expected**
`Show Granted` แสดงเฉพาะ resource ที่มี action ถูกเลือกอย่างน้อย 1; `Show Missing` แสดงเฉพาะ resource ที่ยังเลือกไม่ครบ; `Show All` แสดงทุก resource

---
## TC-ROLE-400003 — ค้นหา resource ใน permission picker กรองเฉพาะที่ตรงคำ
**Priority:** Medium · **Test Type:** Functional
**Preconditions**
อยู่ในโหมด add หรือ edit; catalog สิทธิ์โหลดแล้ว
**Steps**
1. พิมพ์คำค้น resource ในช่องค้นหาของ picker (เช่นชื่อ category หรือ resource)
2. กดปุ่มล้าง (X) เพื่อล้างคำค้น
**Expected**
ระหว่างพิมพ์ matrix แสดงเฉพาะ category/resource ที่ตรงคำค้น (ถ้าไม่ตรงเลยแสดง empty state "No match"); หลังกดล้างกลับมาแสดงทุก resource

---
</content>
</invoke>
